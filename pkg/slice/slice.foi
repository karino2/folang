package_info slice =
  let Length<T>: []T -> int
  let Last<T>: []T -> T
  let Head<T>: []T -> T
  let Take<T> : int->[]T->[]T
  let Map<T, U> : (T->U)->[]T->[]U
  let Filter<T> : (T->bool)->[]T->[]T

