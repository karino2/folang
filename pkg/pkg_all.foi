package_info frt =
  let Println: string->()
  let OpPlus<T>: T->T->T
  let OpMinus<T>: T->T->T

package_info buf =
  type Buffer
  let New: ()->Buffer
  let Write: Buffer->string->()
  let String: Buffer->string

package_info slice =
  let Length<T>: []T -> int
  let Take<T> : int->[]T->[]T
  let Map<T, U> : (T->U)->[]T->[]U

